import React from "react";
import TitleText from "../components/common/TitleText";

const PartnersPage = () => {
  return (
    <a name="partners" style={{ pointerEvents: "none", cursor: "default"}}>
      <div>
        <TitleText text="Partnerzy" />
        Partners Page
      </div>
    </a>
  );
};

export default PartnersPage;
